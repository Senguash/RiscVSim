#include <stdio.h>
#include <stdlib.h>

typedef long word;
typedef int hWord;
typedef char byte;
typedef long uWord;
typedef int uHWord;
typedef char uByte;

#define BYTE_TO_BINARY_PATTERN "%c%c%c%c%c%c%c%c"
#define BYTE_TO_BINARY(byte)  \
  ((byte) & 0x80 ? '1' : '0'), \
  ((byte) & 0x40 ? '1' : '0'), \
  ((byte) & 0x20 ? '1' : '0'), \
  ((byte) & 0x10 ? '1' : '0'), \
  ((byte) & 0x08 ? '1' : '0'), \
  ((byte) & 0x04 ? '1' : '0'), \
  ((byte) & 0x02 ? '1' : '0'), \
  ((byte) & 0x01 ? '1' : '0')


int main(){
    word ha = 0xaaaaaaaa;
	printf("hello world\n");
}
